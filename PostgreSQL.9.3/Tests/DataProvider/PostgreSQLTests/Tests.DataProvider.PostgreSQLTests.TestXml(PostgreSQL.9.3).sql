-- PostgreSQL.9.3 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.9.3 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.9.3 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.9.3 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml/>'

SELECT @p

-- PostgreSQL.9.3 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.9.3 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.9.3 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.9.3 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

