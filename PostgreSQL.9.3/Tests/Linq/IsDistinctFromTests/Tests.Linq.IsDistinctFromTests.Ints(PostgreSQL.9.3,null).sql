-- PostgreSQL.9.3 PostgreSQL
SELECT
	COUNT(*)
FROM
	"Src" s

-- PostgreSQL.9.3 PostgreSQL
SELECT
	COUNT(*)
FROM
	"Src" s
WHERE
	s."NullableInt" IS NOT NULL

-- PostgreSQL.9.3 PostgreSQL
SELECT
	COUNT(*)
FROM
	"Src" s
WHERE
	1 = 0

-- PostgreSQL.9.3 PostgreSQL
SELECT
	COUNT(*)
FROM
	"Src" s
WHERE
	s."NullableInt" IS NULL

