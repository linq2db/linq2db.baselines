-- Oracle.11.Managed Oracle11

SELECT
	LISTAGG(t1."Value2", ' -> ') WITHIN GROUP (ORDER BY t1."Id")
FROM
	"SampleClass" t1

-- Oracle.11.Managed Oracle11

SELECT
	LISTAGG(t1."Value2", ' -> ') WITHIN GROUP (ORDER BY t1."Id")
FROM
	"SampleClass" t1

