-- PostgreSQL.18 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.18 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.18 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.18 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml/>'

SELECT @p

-- PostgreSQL.18 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.18 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.18 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.18 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

