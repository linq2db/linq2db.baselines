BeforeExecute
-- Informix.DB2 Informix (asynchronously)

SELECT
	COUNT(*)
FROM
	Src s
WHERE
	s.Enum IN (NULL, NULL)

BeforeExecute
-- Informix.DB2 Informix (asynchronously)

SELECT
	COUNT(*)
FROM
	Src s
WHERE
	s.Enum NOT IN (NULL, NULL)

