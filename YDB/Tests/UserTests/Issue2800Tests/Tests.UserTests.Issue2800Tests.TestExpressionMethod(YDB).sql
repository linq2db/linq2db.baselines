-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x
WHERE
	x.Name <> 'Special'u OR x.Name IS NULL

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x
WHERE
	x.Name = 'Special'u

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x
WHERE
	x.Name <> 'Special'u OR x.Name IS NULL

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

-- YDB Ydb

SELECT
	x.Id as Id,
	x.Name as Name
FROM
	Car x
WHERE
	x.Name = 'Special'u

-- YDB Ydb

SELECT
	t1.Id as Id,
	t1.Name as Name
FROM
	Car t1

