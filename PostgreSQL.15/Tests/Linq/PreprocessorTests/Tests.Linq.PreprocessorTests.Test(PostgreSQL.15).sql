-- PostgreSQL.15 PostgreSQL

SELECT
	p."ParentID"
FROM
	"Parent" p
WHERE
	p."ParentID" = 2
LIMIT 1

-- PostgreSQL.15 PostgreSQL

SELECT
	p."ParentID"
FROM
	"Parent" p
WHERE
	p."ParentID" = 2
LIMIT 1

-- PostgreSQL.15 PostgreSQL

SELECT
	p."ParentID"
FROM
	"Parent" p
WHERE
	p."ParentID" = 2
LIMIT 1

