-- PostgreSQL.15 PostgreSQL13

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.15 PostgreSQL13

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.15 PostgreSQL13

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.15 PostgreSQL13
DECLARE @p Xml
SET     @p = '<xml/>'

SELECT @p

-- PostgreSQL.15 PostgreSQL13
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.15 PostgreSQL13
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.15 PostgreSQL13
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.15 PostgreSQL13
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

