-- ClickHouse.MySql ClickHouse

SELECT
	g_1.LastName,
	0,
	toString(NULL),
	toString(NULL)
FROM
	Person g_1
GROUP BY
	g_1.LastName

-- ClickHouse.MySql ClickHouse

SELECT
	g_1.LastName,
	0,
	toString(NULL),
	toString(NULL)
FROM
	Person g_1
GROUP BY
	g_1.LastName

