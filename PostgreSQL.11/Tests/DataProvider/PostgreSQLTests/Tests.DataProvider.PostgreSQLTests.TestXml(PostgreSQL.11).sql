-- PostgreSQL.11 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.11 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.11 PostgreSQL
SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.11 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml/>'

SELECT @p

-- PostgreSQL.11 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.11 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.11 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.11 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

