-- DuckDB

SELECT
	p.PersonID,
	p.FirstName
FROM
	Person p

-- DuckDB

SELECT
	p.PersonID,
	p.FirstName
FROM
	Person p

-- DuckDB

SELECT
	p.PersonID,
	p.FirstName
FROM
	Person p

-- DuckDB

SELECT
	p.PersonID,
	p.FirstName
FROM
	Person p

-- DuckDB

SELECT
	p.PersonID,
	p.FirstName
FROM
	Person p

