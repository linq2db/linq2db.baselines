-- DuckDB

SELECT
	'test'

-- DuckDB

SELECT
	'test'

-- DuckDB

SELECT
	'test'

-- DuckDB

SELECT
	LPAD('test', 5, '.')

-- DuckDB

SELECT
	LPAD('test', 6, ' ')

-- DuckDB

SELECT
	LPAD('test', 6, ' ')

-- DuckDB

SELECT
	LPAD('test', 16, '.')

