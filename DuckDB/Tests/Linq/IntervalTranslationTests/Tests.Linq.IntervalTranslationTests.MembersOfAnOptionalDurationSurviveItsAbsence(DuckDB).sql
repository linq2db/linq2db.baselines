-- DuckDB
SELECT
	r.Id,
	CAST(((CAST(r.Grace AS BIGINT) * 10000000) // 600000000) % 60 AS INTEGER),
	CAST(CAST(r.Grace AS BIGINT) * 10000000 AS DOUBLE) / 600000000,
	CAST(CAST(r.Required AS BIGINT) * 10000000 AS DOUBLE) / 600000000
FROM
	OptionalDurationRow r
ORDER BY
	r.Id

