-- DuckDB

SELECT
	ch.ParentID,
	ch.ChildID
FROM
	Child ch
LIMIT 2

-- DuckDB

SELECT
	ch.ParentID,
	ch.ChildID
FROM
	Child ch
LIMIT 3

-- DuckDB

SELECT
	ch.ParentID,
	ch.ChildID
FROM
	Child ch
LIMIT 2

-- DuckDB

SELECT
	ch.ParentID,
	ch.ChildID
FROM
	Child ch
LIMIT 3

