-- DuckDB

SELECT
	MAX(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- DuckDB

SELECT
	MAX(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- DuckDB

SELECT
	MAX(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- DuckDB

SELECT
	MAX(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

