-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x
WHERE
	x.Name <> 'Special' OR x.Name IS NULL

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x
WHERE
	x.Name = 'Special'

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x
WHERE
	x.Name <> 'Special' OR x.Name IS NULL

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

-- DuckDB

SELECT
	x.Id,
	x.Name
FROM
	Car x
WHERE
	x.Name = 'Special'

-- DuckDB

SELECT
	t1.Id,
	t1.Name
FROM
	Car t1

