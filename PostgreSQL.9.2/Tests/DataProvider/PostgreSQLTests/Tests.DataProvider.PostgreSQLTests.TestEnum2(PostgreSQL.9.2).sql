-- PostgreSQL.9.2 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.9.2 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'B'

SELECT @p

-- PostgreSQL.9.2 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.9.2 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.9.2 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

