-- Sybase.Managed Sybase

SELECT
	[p].[PersonID],
	[d].[PersonID]
FROM
	[Person] [p]
		INNER JOIN [Doctor] [d] ON [p].[PersonID] = [d].[PersonID]

-- Sybase.Managed Sybase

SELECT
	[p].[PersonID],
	[d].[PersonID]
FROM
	[Person] [p]
		INNER JOIN [Doctor] [d] ON [p].[PersonID] = [d].[PersonID]

