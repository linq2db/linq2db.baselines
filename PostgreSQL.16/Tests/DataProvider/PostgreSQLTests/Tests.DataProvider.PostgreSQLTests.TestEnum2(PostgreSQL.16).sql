-- PostgreSQL.16 PostgreSQL.15 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.16 PostgreSQL.15 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'B'

SELECT @p

-- PostgreSQL.16 PostgreSQL.15 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.16 PostgreSQL.15 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

-- PostgreSQL.16 PostgreSQL.15 PostgreSQL
DECLARE @p Text(1) -- String
SET     @p = 'A'

SELECT @p

