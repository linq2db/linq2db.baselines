-- SQLite.MS SQLite

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- SQLite.MS SQLite

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- SQLite.MS SQLite

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- SQLite.MS SQLite

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

