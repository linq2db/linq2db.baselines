-- SQLite.MS SQLite

SELECT
	COUNT(*)
FROM
	[Src] [x]
WHERE
	[x].[A] IS NULL

-- SQLite.MS SQLite

SELECT
	COUNT(*)
FROM
	[Src] [x]
WHERE
	[x].[A] IS NULL

-- SQLite.MS SQLite

SELECT
	COUNT(*)
FROM
	[Src] [x]
WHERE
	[x].[A] IS NULL

