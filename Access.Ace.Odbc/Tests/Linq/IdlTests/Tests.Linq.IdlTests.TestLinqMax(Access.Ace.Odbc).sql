-- Access.Ace.Odbc AccessODBC

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.Odbc AccessODBC

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.Odbc AccessODBC

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.Odbc AccessODBC

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

