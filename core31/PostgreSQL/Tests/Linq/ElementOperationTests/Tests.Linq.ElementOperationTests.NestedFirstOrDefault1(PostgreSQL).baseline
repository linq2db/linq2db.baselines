BeforeExecute
-- PostgreSQL PostgreSQL.9.5 PostgreSQL
DECLARE @take Integer -- Int32
SET     @take = 1

SELECT
	t2."ChildID",
	t2."ParentID"
FROM
	"Parent" p
		LEFT JOIN LATERAL (
			SELECT
				t1."ChildID",
				t1."ParentID"
			FROM
				"Child" t1
			LIMIT :take
		) t2 ON 1=1

