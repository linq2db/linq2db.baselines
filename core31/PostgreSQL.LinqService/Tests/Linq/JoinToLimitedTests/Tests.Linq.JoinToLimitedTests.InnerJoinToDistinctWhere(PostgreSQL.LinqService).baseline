BeforeExecute
-- PostgreSQL PostgreSQL.9.5 PostgreSQL

SELECT
	o."ParentID",
	o."Value1",
	t1."ChildID",
	t1."ParentID"
FROM
	"Parent" o
		INNER JOIN LATERAL (
			SELECT DISTINCT
				x."ChildID",
				x."ParentID"
			FROM
				"Child" x
			WHERE
				x."ParentID" = o."ParentID"
		) t1 ON 1=1

