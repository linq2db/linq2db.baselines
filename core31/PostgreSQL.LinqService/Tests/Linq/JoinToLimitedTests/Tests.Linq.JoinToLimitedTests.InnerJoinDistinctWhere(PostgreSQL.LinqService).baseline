BeforeExecute
-- PostgreSQL PostgreSQL.9.5 PostgreSQL

SELECT
	o."ParentID",
	o."Value1",
	x."ChildID",
	x."ParentID"
FROM
	"Parent" o
		INNER JOIN (
			SELECT DISTINCT
				t1."ChildID",
				t1."ParentID"
			FROM
				"Child" t1
		) x ON x."ParentID" = o."ParentID"

