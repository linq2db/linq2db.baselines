-- PostgreSQL.13 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.13 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.13 PostgreSQL

SELECT XMLPARSE (DOCUMENT'<xml/>')

-- PostgreSQL.13 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml/>'

SELECT @p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Xml
SET     @p = '<xml />'

SELECT @p

