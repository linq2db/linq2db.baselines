-- PostgreSQL.13 PostgreSQL
DECLARE @p Integer -- Int32
SET     @p = 1

SELECT
	p."ParentID",
	p."Value1"
FROM
	"Parent" p
WHERE
	p."ParentID" = :p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Integer -- Int32
SET     @p = 1

SELECT
	p."ParentID",
	p."Value1"
FROM
	"Parent" p
WHERE
	p."ParentID" = :p

-- PostgreSQL.13 PostgreSQL
DECLARE @p Integer -- Int32
SET     @p = 1

SELECT
	p."ParentID",
	p."Value1"
FROM
	"Parent" p
WHERE
	p."ParentID" = :p

