BeforeExecute
-- SQLite.MS SQLite

SELECT
	[r_1].[PersonID]
FROM
	[Person] [t1]
		LEFT JOIN (
			SELECT
				0 as [PersonID]
			FROM
				[Doctor] [r]
		) [r_1] ON 1 = 0

BeforeExecute
-- SQLite.MS SQLite

SELECT
	[r_1].[PersonID]
FROM
	[Person] [t1]
		LEFT JOIN (
			SELECT
				0 as [PersonID]
			FROM
				[Doctor] [r]
		) [r_1] ON 1 = 0

