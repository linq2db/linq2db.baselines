-- ClickHouse.Driver ClickHouse

SELECT
	maxOrNull(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- ClickHouse.Driver ClickHouse

SELECT
	maxOrNull(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- ClickHouse.Driver ClickHouse

SELECT
	maxOrNull(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

-- ClickHouse.Driver ClickHouse

SELECT
	maxOrNull(x.PersonID)
FROM
	Patient x
WHERE
	x.PersonID < 0

