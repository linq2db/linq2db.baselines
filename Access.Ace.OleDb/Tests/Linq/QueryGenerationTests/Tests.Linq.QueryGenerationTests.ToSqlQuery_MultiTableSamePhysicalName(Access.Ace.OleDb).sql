-- Access.Ace.OleDb AccessOleDb
SELECT
	[p].[PersonID],
	[d].[PersonID] as [PersonID_1]
FROM
	[Person] [p]
		INNER JOIN [Doctor] [d] ON ([p].[PersonID] = [d].[PersonID])

-- Access.Ace.OleDb AccessOleDb
SELECT
	[p].[PersonID],
	[d].[PersonID] as [PersonID_1]
FROM
	[Person] [p]
		INNER JOIN [Doctor] [d] ON ([p].[PersonID] = [d].[PersonID])

