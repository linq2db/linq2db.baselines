-- Access.Ace.OleDb AccessOleDb

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.OleDb AccessOleDb

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.OleDb AccessOleDb

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

-- Access.Ace.OleDb AccessOleDb

SELECT
	MAX([x].[PersonID])
FROM
	[Patient] [x]
WHERE
	[x].[PersonID] < 0

