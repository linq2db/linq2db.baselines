-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	t."Value"

-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	t."Value" = 't'::BOOLEAN

-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	t."Value" = 'f'::BOOLEAN

-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	NOT t."Value"

-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	t."Value" = 'f'::BOOLEAN

-- Informix.DB2 Informix

SELECT
	t.Id,
	t."Value"
FROM
	NotNullableBoolClass t
WHERE
	t."Value" = 't'::BOOLEAN

